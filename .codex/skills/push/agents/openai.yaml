interface:
  display_name: "Git Monitor Push"
  short_description: "Publica uma nova versao patch do Git Monitor"
  default_prompt: "Use $git-monitor-push para publicar uma nova versao patch do Git Monitor via validacoes locais, build Electron, commit, tag e push para origin/master."

policy:
  allow_implicit_invocation: false
